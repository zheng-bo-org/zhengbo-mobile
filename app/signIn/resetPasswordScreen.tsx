import {Text, View} from "react-native";

export default function ResetPwdScreen() {
   return <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>
          I am the reset pwd screen.
      </Text>
   </View>
}